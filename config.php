<?php

return array(
//	'images_dir' => 'content/images/', // "Absolute" = uses current page location as directory with full URL, "Relative" simply ommits the domain and TLD, or specify a single folder where all images are located (such as "content/images/")
	'wrap_element' => 'div', // the element to use for img backgrounds
	'wrap_class_img' => 'content-image', // the class applied to image elements
	'wrap_class_vid' => 'content-video' // the class applied to video elements
	);
